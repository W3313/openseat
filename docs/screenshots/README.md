# Screenshots (placeholders)

The README embeds these files; record them from the running app in dark mode (MUST + finished SHOULD paths only) and drop them here with exactly these names:

| File | What to capture |
|---|---|
| `hero.gif` | 12 s: landing → CS rankings → expand a card → professor detail page. |
| `rankings-card.png` | One expanded `ProfessorCard` on `/s/uiuc/CS` (rating, dots, GPA, delta chip, grade bar, sparkline, badges). |
| `detail-page.png` | `/p/uiuc/<slug>` header + stats grid + grade bar + GPA-by-year chart. |
| `ai-panel.png` | The full `AISummaryPanel` with the source pill visible. |
| `about-matching.png` | `/about#matching`: tier table + coverage stats + match-report table. |
| `og-unfurl.png` | The Open Graph image for `/s/uiuc/CS` (1200×630) as it unfurls in a chat client. |
| `lighthouse.png` | Lighthouse report for `/s/uiuc/CS` on a mobile profile. |

Until they exist the README shows broken-image alt text, which is intentional: nothing here is fabricated.
