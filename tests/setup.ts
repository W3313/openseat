// Global vitest setup (SPEC 12.2): registers the jest-dom matchers (toBeInTheDocument, toHaveTextContent, ...)
// on `expect`. Component tests additionally opt into jsdom with `// @vitest-environment jsdom` on line 1.
import '@testing-library/jest-dom/vitest';
